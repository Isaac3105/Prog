{
    "version": "2.0.0",
    "tasks": [
    {
        "label": "build active file",
        "type": "shell",
        "command": "g++",
        "args": [
            "-g",
            "${file}",
            "-o",
            "${fileDirname}/${fileBasenameNoExtension}.out"
        ],
        "group": "build",
        "problemMatcher": ["$gcc"],
        "detail": "Build the currently open C++ file"
    }
    ]
}